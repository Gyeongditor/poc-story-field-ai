# Core AI libraries
transformers==4.44.2
diffusers==0.31.0
accelerate==0.33.0
safetensors==0.4.4

# Tokenizer dependencies
sentencepiece==0.2.1
protobuf==6.31.1

# Optional: 이미지 생성 품질 향상 관련
Pillow>=9.0.0

# CUDA 환경에서는 torch를 requirements에 넣지 않음
# (AWS GPU 환경에 맞는 torch는 컨테이너 베이스 이미지에서 제공)
